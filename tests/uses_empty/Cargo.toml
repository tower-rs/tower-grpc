[package]
name = "uses_empty"
version = "0.1.0"
authors = ["Carl Lerche <me@carllerche.com>"]
publish = false

[dependencies]
tower-grpc = { path = "../../" }

[build-dependencies]
tower-grpc-build = { path = "../../tower-grpc-build" }
