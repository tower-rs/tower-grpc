[package]
name = "uses_empty"
version = "0.1.0"
authors = ["Carl Lerche <me@carllerche.com>"]
publish = false

[dependencies]
prost-derive = "0.4"
tower-grpc = { path = "../../" }

[build-dependencies]
tower-grpc-build = { path = "../../tower-grpc-build" }
